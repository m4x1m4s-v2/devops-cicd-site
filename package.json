{
  "name": "devops-cicd-site",
  "version": "1.0.0",
  "description": "Simple static site deployed to S3 + CloudFront via GitHub Actions",
  "private": true,
  "scripts": {
    "test": "node test.js",
    "build": "node build.js"
  }
}
